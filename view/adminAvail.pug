button.back.btn.btn-success
  a(href="/dashboard") Back To Dashboard
button.logout.btn.btn-danger
  a(href="/auth/logout") Log Out

.outer
    .card
        .card-header Employee List
        .card-body
            form(id='getEmployee')
                .form-group
                    label(for='selectEmployee') Select Employee
                    select#employee-list.form-control(name = "employeeId")
                        option(selected) Select Employee
                button.btn.btn-primary(type='submit') Submit
    .card
        .card-header Their Availability Schedule
        .card-body
          #table.table.table-striped
            table.table.table-striped
              thead.thead-dark
                tr
                  th(scope="col") Days
                  th(scope="col") Date
                  th(scope="col") Time Range
                  th(scope="col") Time Zone
              tbody
              